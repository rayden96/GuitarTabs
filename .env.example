# Neon Postgres connection string (from Vercel Marketplace integration or neon.tech)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# The single password that protects the app's sync API
APP_PASSWORD=choose-a-strong-password
